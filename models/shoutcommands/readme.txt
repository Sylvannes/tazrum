To implement shout commands, name your class "<Name>Command", where <Name> is
the typed command. For example, the command:

/debug

Resolves to the DebugCommand.

Furthermore, be sure to implement the ShoutCommand interface.